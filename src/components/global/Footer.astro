---
import { footerNav } from '@data/navigation';
import { site, url } from '@data/site';
---

<footer class="footer section">
  <div class="container">
    <div class="footer__grid">
      <!-- Brand -->
      <div class="footer__brand">
        <span class="footer__logo-text">MASTER<strong>TETTO</strong></span>
        <p class="footer__tagline">{site.tagline}</p>
        <p class="footer__address">
          {site.address.street}<br />
          {site.address.postalCode} {site.address.city} ({site.address.province})
        </p>
      </div>

      <!-- Navigation -->
      <div class="footer__nav">
        <h4 class="footer__heading">Navigazione</h4>
        <ul role="list">
          {
            footerNav.map((link) => (
              <li>
                <a href={link.href} class="footer__link">
                  {link.label}
                </a>
              </li>
            ))
          }
        </ul>
      </div>

      <!-- Contact -->
      <div class="footer__contact">
        <h4 class="footer__heading">Contatti</h4>
        <ul role="list">
          <li>
            <a href={`tel:${site.phone}`} class="footer__link">{site.phoneDisplay}</a>
          </li>
          <li>
            <a href={`mailto:${site.email}`} class="footer__link">{site.email}</a>
          </li>
          <li>
            <a href={site.whatsapp} class="footer__link" target="_blank" rel="noopener">
              WhatsApp
            </a>
          </li>
        </ul>
      </div>

      <!-- Social -->
      <div class="footer__social">
        <h4 class="footer__heading">Social</h4>
        <div class="footer__social-links">
          {
            site.socialLinks.map((social) => (
              <a
                href={social.url}
                class="footer__social-link"
                target="_blank"
                rel="noopener"
                aria-label={social.label}
              >
                {social.label}
              </a>
            ))
          }
        </div>
      </div>
    </div>

    <hr class="divider" />

    <div class="footer__bottom">
      <p class="footer__legal">
        &copy; {new Date().getFullYear()} {site.legalName} &mdash; P.IVA {site.piva}
      </p>
      <div class="footer__bottom-links">
        <a href={url('/privacy/')} class="footer__link">Privacy Policy</a>
        <a href={url('/cookie/')} class="footer__link">Cookie Policy</a>
      </div>
    </div>
  </div>
</footer>

<style>
  .footer {
    background: var(--bg-secondary);
    border-top: 1px solid var(--border);
  }

  .footer__grid {
    display: grid;
    grid-template-columns: 2fr 1fr 1fr 1fr;
    gap: var(--space-48);
    margin-bottom: var(--space-48);
  }

  .footer__logo-text {
    font-family: var(--font-body);
    font-size: 1.1rem;
    font-weight: var(--weight-light);
    letter-spacing: var(--tracking-wider);
    color: var(--text-primary);
    text-transform: uppercase;
  }

  .footer__logo-text strong {
    font-weight: var(--weight-bold);
    color: var(--accent-primary);
  }

  .footer__tagline {
    margin-top: var(--space-8);
    font-size: var(--text-sm);
    color: var(--text-muted);
  }

  .footer__address {
    margin-top: var(--space-16);
    font-size: var(--text-sm);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
  }

  .footer__heading {
    font-family: var(--font-body);
    font-size: var(--text-sm);
    font-weight: var(--weight-semibold);
    letter-spacing: var(--tracking-wide);
    text-transform: uppercase;
    color: var(--text-primary);
    margin-bottom: var(--space-16);
  }

  .footer__link {
    display: block;
    padding-block: var(--space-4);
    font-size: var(--text-sm);
    color: var(--text-secondary);
    transition: color var(--duration-fast);
  }

  .footer__link:hover {
    color: var(--accent-primary);
  }

  .footer__social-links {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .footer__social-link {
    font-size: var(--text-sm);
    color: var(--text-secondary);
    transition: color var(--duration-fast);
  }

  .footer__social-link:hover {
    color: var(--accent-primary);
  }

  .footer__bottom {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-top: var(--space-32);
  }

  .footer__legal {
    font-size: var(--text-xs);
    color: var(--text-muted);
  }

  .footer__bottom-links {
    display: flex;
    gap: var(--space-24);
  }

  @media (max-width: 768px) {
    .footer__grid {
      grid-template-columns: 1fr 1fr;
      gap: var(--space-32);
    }

    .footer__brand {
      grid-column: 1 / -1;
    }

    .footer__bottom {
      flex-direction: column;
      gap: var(--space-16);
      text-align: center;
    }
  }

  @media (max-width: 480px) {
    .footer__grid {
      grid-template-columns: 1fr;
    }
  }
</style>
